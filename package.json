{
  "name": "actions",
  "version": "1",
  "description": "Monorepo for custom actions",
  "scripts": {
    "build": "for D in *; do if [ -f \"${D}/package.json\" ]; then ( cd \"$D\" && npm run all ); fi; done",
    "test": "npm run build; git diff --exit-code"
  },
  "dependencies": {
    "@actions/artifact": "^1.1.1",
    "@actions/core": "^1.10.0",
    "@actions/exec": "^1.1.1",
    "@actions/io": "^1.1.3"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1",
    "eslint": "^8.39.0"
  }
}
