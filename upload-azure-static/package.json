{
  "name": "@actions/upload-azure-static",
  "version": "1",
  "description": "Upload static site to Azure",
  "main": "dist/index.js",
  "scripts": {
    "lint": "eslint .",
    "prepare": "ncc build index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "all": "npm run lint && npm run prepare"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/exec": "^1.1.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1",
    "eslint": "^8.39.0"
  }
}
